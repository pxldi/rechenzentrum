---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# No `namespace:` here on purpose — this RoleBinding targets a namespace other
# than claudebox, and a namespace transformer would rewrite it.
resources:
  - rolebindings.yaml
