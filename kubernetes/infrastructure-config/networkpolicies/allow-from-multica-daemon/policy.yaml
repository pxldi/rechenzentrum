apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-from-multica-daemon
spec:
  podSelector: {}
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: multica-daemon
  policyTypes:
    - Ingress
