---
# The agent's identity. Its only grant is reading Traefik's pod log in the
# traefik namespace (networking/traefik/crowdsec-log-reader.yaml).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: crowdsec-agent
  namespace: crowdsec
