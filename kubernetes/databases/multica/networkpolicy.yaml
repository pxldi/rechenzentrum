---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-cnpg-operator
  namespace: multica
spec:
  podSelector:
    matchLabels:
      cnpg.io/cluster: multica-postgresql
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: cnpg-system
  policyTypes:
    - Ingress
