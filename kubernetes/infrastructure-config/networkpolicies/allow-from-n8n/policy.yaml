apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-from-n8n
spec:
  podSelector: {}
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: n8n
  policyTypes:
    - Ingress
